export interface ModelProps {
    cover_image_url: string;
    name: string;
    owner: string;
    createdAt: string;
    description: string;
}

export interface ModelListCollectionType {
    description: string;
    name: string;
    slug: string;

}